package learning;

import java.util.Scanner;

public class TotalAmountOfPurchase {

	public static void main(String[] args) {
		@SuppressWarnings("resource")
		Scanner scanner = new Scanner (System.in);	
		
		System.out.print("Enter rate of item: ");
		int rate = scanner.nextInt();
		System.out.print("Enter quantity of item: ");
		int quantity = scanner.nextInt();
		
		int totalamount = rate*quantity;
		int discountamount = totalamount*15/100;
		
		if (totalamount>=2000) {
			double netamount = totalamount*15/100 - discountamount;
			System.out.print("net amount: " + netamount);
		}
		else {
			double netamount = totalamount*15/100 - discountamount;
			System.out.print("net amount: " + netamount);
		}
	}

}
